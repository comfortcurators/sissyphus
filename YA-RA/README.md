# YA|RA

**rv0.3.0** — MIT. Installable. Not VirusTotal YARA.

```
pip install -e YA-RA
ya-ra measure YA-RA/programs/primer --root YA-RA/programs/primer
ya-ra compile YA-RA/programs/cura --root YA-RA/programs/cura --to wasm
```

Without install: `PYTHONPATH=YA-RA/src python3 -m ya_ra measure --root .`

Name is **YA|RA**. Path `.YA-RA`. Package `ya_ra`. Signed is envelope.

Cura weaves what-is and what-became onto one plane and writes `aforementioned.YA-RA`.
Wasm bakes that plane (`measure`, `cura`). exists/run stay refused, same mouth as Python.

DOI of the language is not minted yet. Deposit from `yashrajvansh/YA-RA-language`.
GitHub release tag: `rv0.3.0` on this repo. Linguist still needs usage.
