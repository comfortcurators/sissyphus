# YA|RA rv0.1.0 — past the single action

The slide titled *The theory of everything (so far)* writes one number:

$$
Z=\int\mathcal{D}(\mathrm{Fields})\,\exp\!\left(i\int d^{4}x\sqrt{-g}\,\mathcal{L}\right)
$$

with $\mathcal{L}$ the Einstein–Hilbert term, the three Yang–Mills kinetic terms, Dirac, Higgs kinetic, Higgs potential, Yukawa. Gravity plus the Standard Model, packed into one path integral.

That is a *total*. It sums every field configuration the Lagrangian can name. It has no observer, no cut, no attributed attempt. It cannot fail as a door. It can only be approximately computed.

YA|RA does not replace $R$, $F_{\mu\nu}$, or $\psi$. It refuses the idea that the next language is a larger Lagrangian.

## The cut

```
Intent | Pattern
```

The bar is the measurement. Left side is what may become true. Right side is how the world is allowed to say no. `Signed` names the apparatus.

TOE integrates. YA|RA *projects*.

| TOE (so far) | YA|RA rv0.1.0 |
| --- | --- |
| one action $S[\varphi]$ | many attributed Intents |
| $\int\mathcal{D}\varphi\,e^{iS}$ | no integral over unsigned claims |
| fields are the ontology | the glimpse is the ontology |
| no observer in $Z$ | Signed is required |
| failure is a bad approximation | failure is a first-class eigenvalue |

## State

A door before `check` is unmeasured. Checks are modes of Pattern. Running them is collapse:

- `measure all` — product of projectors. One fail contradicts the Intent.
- `measure any` — sum of projectors. One pass keeps the Intent.

Amplitudes (`amp`) may be written on a check. rv0.1.0 records them. It does not pretend they are Born probabilities. Born would be a later rv, and only after a Pattern that actually returns a complex amplitude.

## What “past” means

Past TOE is not “add quantum gravity to the slide.” Past TOE is: stop writing the universe as one unattributed integral, and start writing wagers that a named observer can lose.

Input → Logic → Output stays the founder diagram.

what is → change → what becomes

Inevitability is observed through a glimpse, not through a complete $\mathcal{L}$.
