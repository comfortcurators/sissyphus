[project]
name = "ya-ra"
version = "0.1.0"
description = "YA|RA rv0.1.0 — Intent | Pattern | Signed. Version token always rv."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
ya-ra = "ya_ra.__main__:main"
